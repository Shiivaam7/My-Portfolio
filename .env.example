# Frontend (Vercel) — public URL of your Express API on Render (no trailing slash)
NEXT_PUBLIC_CONTACT_API_URL=http://localhost:5000

# Optional: production site URL for metadata
NEXT_PUBLIC_SITE_URL=
